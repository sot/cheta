# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Denote files that are truly binary and should not be modified.
*.pkl binary

# for setuptools_scm_git_archive
.git_archival.txt  export-subst